import { AnimationDirectivesDirective } from './animation-directives.directive';

describe('AnimationDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
